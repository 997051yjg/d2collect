/* =========================================
   Mode 1: 寻觅模式 (Seek Mode)
   ========================================= */
.seek-mode {
  padding-top: 100rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.seek-header {
  display: flex;
  flex-direction: column;
  align-items: center;
  margin-bottom: 60rpx;
}

.horadric-icon {
  width: 120rpx; height: 120rpx;
  margin-bottom: 24rpx;
  filter: drop-shadow(0 0 20rpx rgba(212, 175, 55, 0.4));
  animation: floatIcon 4s ease-in-out infinite;
}

@keyframes floatIcon {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-10rpx); }
}

.seek-title {
  font-size: 40rpx; font-weight: 700; color: #fff;
  letter-spacing: 2rpx; margin-bottom: 12rpx;
  text-shadow: 0 2rpx 10rpx rgba(0,0,0,0.8);
}

.seek-subtitle {
  font-size: 26rpx; color: var(--text-secondary);
}

/* 搜索框增强 */
.search-core {
  width: 100%;
  margin-bottom: 40rpx;
  padding: 4rpx;
}

.ios-search-bar.large {
  height: 96rpx;
  background: rgba(0,0,0,0.3);
  border-radius: 16rpx;
}

.ios-search-input { font-size: 30rpx; }

/* 物品栏结果网格 */
.inventory-grid { width: 100%; }

.grid-label {
  font-size: 24rpx; color: var(--text-secondary);
  margin-bottom: 24rpx; text-align: center;
}

.inventory-item {
  display: flex; align-items: center;
  padding: 24rpx;
  margin-bottom: 20rpx;
  transition: all 0.2s ease;
  border: 1rpx solid transparent;
}

.inventory-item:active { transform: scale(0.98); background: rgba(255,255,255,0.1); }

/* 列表里的发光效果 */
.unique-glow { border-color: rgba(212, 175, 55, 0.3); box-shadow: inset 0 0 20rpx rgba(212, 175, 55, 0.05); }
.set-glow { border-color: rgba(50, 205, 50, 0.3); box-shadow: inset 0 0 20rpx rgba(50, 205, 50, 0.05); }

.item-visual {
  width: 80rpx; height: 80rpx;
  background: rgba(0,0,0,0.3);
  border-radius: 8rpx;
  display: flex; align-items: center; justify-content: center;
  margin-right: 24rpx;
}
.item-icon { width: 80%; height: 80%; }

.item-meta { flex: 1; display: flex; flex-direction: column; }
.item-name { font-size: 30rpx; font-weight: 600; margin-bottom: 4rpx; }
.item-type { font-size: 22rpx; color: var(--text-secondary); }

.green-text { color: #32cd32; text-shadow: 0 0 5rpx rgba(50, 205, 50, 0.4); }
.gold-text { color: #d4af37; text-shadow: 0 0 5rpx rgba(212, 175, 55, 0.4); }

.identify-btn {
  font-size: 24rpx; color: var(--gold);
  border: 1rpx solid var(--gold);
  padding: 8rpx 20rpx; border-radius: 30rpx;
}

.empty-guide {
  margin-top: 80rpx; color: var(--text-secondary); font-size: 26rpx; text-align: center;
}

/* =========================================
   Mode 2: 鉴定模式 (Identify Mode)
   ========================================= */
.identify-mode { width: 100%; }

.top-bar {
  display: flex; align-items: center; justify-content: space-between;
  margin-bottom: 32rpx;
}

.back-btn {
  font-size: 24rpx; color: var(--text-secondary);
  padding: 8rpx 20rpx;
}
.back-icon { margin-right: 8rpx; font-size: 28rpx; }

/* ------------------------------------------------
   1. 圣物卡片 (已修正居中)
   ------------------------------------------------ */
.artifact-card {
  padding: 50rpx 40rpx; /* 增加内边距 */
  display: flex;
  flex-direction: column; /* 纵向排列 */
  align-items: center;    /* 水平居中 */
  margin-bottom: 48rpx;
  border-width: 2rpx;
  position: relative;
  overflow: hidden;
}

.border-gold { border-color: rgba(212, 175, 55, 0.5); box-shadow: 0 0 30rpx rgba(212, 175, 55, 0.1); }
.border-green { border-color: rgba(50, 205, 50, 0.5); box-shadow: 0 0 30rpx rgba(50, 205, 50, 0.1); }

/* 头部布局：强制居中 */
.artifact-header {
  display: flex;
  flex-direction: column; /* 图在上，文在下 */
  align-items: center;    /* 居中 */
  width: 100%;
  z-index: 2;
}

.artifact-icon {
  width: 160rpx; 
  height: 160rpx;
  filter: drop-shadow(0 10rpx 20rpx rgba(0,0,0,0.6));
  margin-bottom: 30rpx; /* 图片与文字的间距 */
  animation: hoverItem 3s ease-in-out infinite; /* 保持原有的悬浮动效 */
}

@keyframes hoverItem {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-8rpx); }
}

.artifact-titles {
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16rpx;
}

.artifact-name { 
  font-size: 40rpx; font-weight: 800; display: block; 
}

/* 标签容器 */
.artifact-tags { 
  display: flex; 
  gap: 16rpx; 
  justify-content: center; 
}

/* ------------------------------------------------
   2. 属性铭文区 (已美化固定属性)
   ------------------------------------------------ */
.inscription-section { margin-bottom: 48rpx; }
.section-title { font-size: 28rpx; color: rgba(212, 175, 55, 0.5)}
.attr-list { display: flex; flex-direction: column; gap: 16rpx; }

/* 可变属性行 (保持高亮风格) */
.attr-row.variable {
  display: flex; align-items: center; justify-content: space-between;
  padding: 24rpx 32rpx;
  border: 1rpx solid rgba(212, 175, 55, 0.2);
  border-radius: 16rpx;
  background: rgba(255, 255, 255, 0.03);
}

.attr-meta { display: flex; flex-direction: column; }
.attr-label { font-size: 28rpx; font-weight: 600; margin-bottom: 4rpx; }
.attr-range { font-size: 20rpx; color: var(--text-secondary); }

.attr-input {
  width: 140rpx; text-align: right;
  font-size: 36rpx; font-weight: 700;
  border-bottom: 2rpx solid var(--gold);
  height: 60rpx;
}
.dim-placeholder { color: rgba(255,255,255,0.1); font-size: 32rpx; }

/* 固定属性行 (蚀刻铭文风格) */
.attr-row.fixed {
  padding: 24rpx 32rpx;
  border-radius: 16rpx;
  background: rgba(0, 0, 0, 0.2); /* 深色内嵌底 */
  border: 1rpx solid rgba(255, 255, 255, 0.05); /* 极细边框 */
  display: flex;
  align-items: center;
  position: relative;
  overflow: hidden;
}

/* 左侧金色装饰条 */
.attr-row.fixed::before {
  content: '';
  position: absolute;
  left: 0; top: 25%; bottom: 25%;
  width: 6rpx;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 0 4rpx 4rpx 0;
}

/* 锁图标暗示不可编辑 */
.attr-row.fixed::after {
  content: '🔒'; 
  font-size: 20rpx;
  margin-left: auto; 
  opacity: 0.2;
  filter: grayscale(100%);
}

.attr-fixed-text { 
  font-size: 26rpx; 
  opacity: 0.9;
  letter-spacing: 1rpx;
  padding-left: 16rpx;
}

.no-attrs { text-align: center; color: var(--text-secondary); font-size: 24rpx; padding: 20rpx; }

/* 影像上传 */
.evidence-section { margin-bottom: 40rpx; }
.upload-zone {
  height: 300rpx;
  border: 2rpx dashed rgba(255,255,255,0.2);
  border-radius: 24rpx;
  display: flex; align-items: center; justify-content: center;
  transition: all 0.3s ease;
  overflow: hidden;
  position: relative;
}
.upload-zone:active { background: rgba(255,255,255,0.05); }

.upload-placeholder { display: flex; flex-direction: column; align-items: center; }
.upload-icon-big { font-size: 64rpx; margin-bottom: 16rpx; opacity: 0.6; }
.upload-hint { font-size: 28rpx; color: var(--gold); margin-bottom: 8rpx; }
.upload-sub { font-size: 22rpx; color: var(--text-secondary); }

.preview-img { width: 100%; height: 100%; }

.upload-actions {
  position: absolute; bottom: 20rpx; right: 20rpx;
}
.action-btn.remove {
  background: rgba(0,0,0,0.6); color: #ff6b6b; font-size: 20rpx;
  backdrop-filter: blur(10px);
  line-height: normal; /* 确保行高正常 */
  padding: 8rpx 16rpx; /* 添加内边距让文字有空间 */
}
/* =========================================
   提交仪式按钮 (Ritual Button - Final Polish)
   ========================================= */
   .submit-bar {
    padding: 0 40rpx;
    margin-top: 40rpx;
  }
  
  /* 按钮基础容器 */
  .submit-btn {
    width: 100%; 
    height: 96rpx;
    border-radius: 48rpx; /* 完美胶囊圆角 */
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
    transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
  }
  
  .btn-text {
    position: relative;
    z-index: 2; /* 确保文字在流光之上 */
    font-size: 32rpx;
    font-weight: 600;
    letter-spacing: 4rpx;
  }
  
  /* --- 状态 A: 未激活/禁用 (纯净玻璃态) --- */
  .submit-btn.disabled {
    /* 关键：半透明背景，去除任何白色 */
    background: rgba(255, 255, 255, 0.05); 
    /* 微弱的玻璃边框 */
    border: 1rpx solid rgba(255, 255, 255, 0.1);
    /* 灰暗文字 */
    color: rgba(255, 255, 255, 0.2); 
    /* 毛玻璃效果 */
    backdrop-filter: blur(10px); 
  }
  
  /* --- 状态 B: 已激活 (暗金流光态) --- */
  .submit-btn:not(.disabled) {
    /* 暗金渐变填充 */
    background: linear-gradient(135deg, rgba(212, 175, 55, 0.9) 0%, rgba(184, 156, 79, 1) 100%);
    /* 金色高亮边框 */
    border: 1rpx solid rgba(255, 215, 0, 0.6);
    /* 深色文字形成对比 */
    color: #000; 
    /* 能量光晕：让按钮看起来在发光 */
    box-shadow: 
      0 0 20rpx rgba(212, 175, 55, 0.4),
      inset 0 2rpx 0 rgba(255, 255, 255, 0.4); /* 顶部高光条 */
  }
  
  /* 激活态的点击反馈：微微缩小 */
  .submit-btn:not(.disabled):active {
    transform: scale(0.96);
    box-shadow: 0 0 10rpx rgba(212, 175, 55, 0.2);
  }
  
  /* --- 流光动画层 --- */
  .btn-shine {
    position: absolute;
    top: 0; left: -100%; width: 100%; height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.5), transparent);
    transform: skewX(-20deg);
    animation: shinePass 3s infinite;
    z-index: 1;
  }
  
  @keyframes shinePass {
    0% { left: -100%; }
    20% { left: 200%; } /* 快速扫过 */
    100% { left: 200%; } /* 停留等待 */
}