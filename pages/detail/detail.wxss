/* pages/detail/detail.wxss */

.detail-container {
  min-height: 100vh;
  /* 使用深色径向渐变模拟光源 */
  background: radial-gradient(circle at 50% 20%, #2a2a2a 0%, #0a0a0a 80%);
  color: #ffffff;
  padding: 30rpx;
}

/* 加载状态 */
.loading-container {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 60vh;
}

.loading-spinner {
  width: 60rpx;
  height: 60rpx;
  border: 4rpx solid #d4af37;
  border-top: 4rpx solid transparent;
  border-radius: 50%;
  animation: spin 1s linear infinite;
  margin-bottom: 20rpx;
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}

.loading-text {
  color: #cccccc;
  font-size: 28rpx;
}

/* 2. 头部卡片：增加玻璃质感和高级边框 */
.equipment-header-layout {
  display: flex;
  padding: 40rpx 30rpx;
  background: rgba(20, 20, 20, 0.8); /* 半透明 */
  backdrop-filter: blur(20px);        /* 毛玻璃模糊 */
  border: 2rpx solid rgba(212, 175, 55, 0.3);
  border-radius: 24rpx;
  margin-bottom: 30rpx;
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
  box-shadow: 
    0 10rpx 40rpx rgba(0, 0, 0, 0.6), /* 深度阴影 */
    inset 0 0 20rpx rgba(212, 175, 55, 0.05); /* 内部微光 */
  align-items: flex-start;
}

/* 左右内容之间的金色竖线 */
.equipment-header-layout::after {
  content: '';
  position: absolute;
  top: 40rpx;
  bottom: 40rpx;
  left: calc(50% - 1rpx);
  width: 2rpx;
  background: linear-gradient(to bottom, 
    transparent 0%, 
    #d4af37 20%, 
    #d4af37 80%, 
    transparent 100%);
}

/* 左右内容之间的金色竖线 */
.equipment-header-layout::after {
  content: '';
  position: absolute;
  top: 40rpx;
  bottom: 40rpx;
  left: calc(50% - 1rpx);
  width: 2rpx;
  background: linear-gradient(to bottom, 
    transparent 0%, 
    #d4af37 20%, 
    #d4af37 80%, 
    transparent 100%);
}

/* 头部区域的流光效果 */
.equipment-header-layout::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(212, 175, 55, 0.1), transparent);
  animation: shimmer 3s infinite;
}

.equipment-header-layout:active {
  transform: scale(0.98);
  border-color: #ffd700;
  box-shadow: 
    0 0 30rpx rgba(255, 215, 0, 0.4),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.2);
}

/* 左侧区域：图片和基本信息 */
.equipment-left-section {
  display: flex;
  flex-direction: column;
  flex: 1;
  margin-right: 40rpx;
  align-items: center;
  justify-content: flex-start;
  margin-top: 10rpx;
}

/* 装备图片（左上角） */
.equipment-image-top-left {
  width: 150rpx;
  height: 150rpx;
  margin-bottom: 20rpx;
  display: flex;
  justify-content: center;
}

.main-equipment-image {
  width: 100%;
  height: 100%;
  object-fit: contain;
  border-radius: 12rpx;
  border: 1rpx solid #d4af37;
  box-shadow: 0 2rpx 8rpx rgba(212, 175, 55, 0.2);
}

/* 基本信息（左下角） */
.equipment-info-bottom-left {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  width: 100%;
}

/* 右侧区域：装备属性 */
.equipment-attributes-right {
  flex: 1;
  min-width: 200rpx;
  padding-left: 30rpx;
}

.equipment-attributes-right .section-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #d4af37;
  margin-bottom: 25rpx;
  border-bottom: 1rpx solid rgba(212, 175, 55, 0.3);
  padding-bottom: 8rpx;
}

.equipment-attributes-right .attributes-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

/* 4. 属性列表优化：像羊皮纸一样的条目 */
.equipment-attributes-right .attribute-item {
  display: flex;
  flex-direction: row;
  align-items: center;
  background: rgba(255, 255, 255, 0.03);
  border-radius: 8rpx;
  padding: 12rpx 16rpx;
  margin-bottom: 12rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.05);
}

.equipment-attributes-right .attribute-item:last-child {
  margin-bottom: 0;
}

.equipment-attributes-right .label {
  color: #cccccc;
  font-size: 28rpx;
  margin-right: 12rpx;
  white-space: nowrap;
}

/* 5. 属性值优化 */
.equipment-attributes-right .value {
  color: #c9b065; /* 使用稍微柔和的暗金色 */
  font-family: monospace; /* 等宽字体更有数据感 */
  font-size: 28rpx;
  font-weight: bold;
  line-height: 1.4;
  text-align: left;
  flex: 1;
}

/* 右侧区域：装备属性 */
.equipment-attributes-right {
  flex: 1;
  min-width: 200rpx;
  padding-left: 30rpx;
}

.equipment-attributes-right .section-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #d4af37;
  margin-bottom: 25rpx;
  border-bottom: 1rpx solid rgba(212, 175, 55, 0.3);
  padding-bottom: 8rpx;
}

.equipment-attributes-right .attributes-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

/* 4. 属性列表优化：像羊皮纸一样的条目 */
.equipment-attributes-right .attribute-item {
  display: flex;
  flex-direction: row;
  align-items: center;
  background: rgba(255, 255, 255, 0.03);
  border-radius: 8rpx;
  padding: 12rpx 16rpx;
  margin-bottom: 12rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.05);
}

.equipment-attributes-right .attribute-item:last-child {
  margin-bottom: 0;
}

.equipment-attributes-right .label {
  color: #cccccc;
  font-size: 28rpx;
  margin-right: 12rpx;
  white-space: nowrap;
}

/* 5. 属性值优化 */
.equipment-attributes-right .value {
  color: #c9b065; /* 使用稍微柔和的暗金色 */
  font-family: monospace; /* 等宽字体更有数据感 */
  font-size: 28rpx;
  font-weight: bold;
  line-height: 1.4;
  text-align: left;
  flex: 1;
}

/* 右侧区域：装备属性 */
.equipment-attributes-right {
  flex: 1;
  min-width: 200rpx;
  padding-left: 30rpx;
}

.equipment-attributes-right .section-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #d4af37;
  margin-bottom: 25rpx;
  border-bottom: 1rpx solid rgba(212, 175, 55, 0.3);
  padding-bottom: 8rpx;
}

.equipment-attributes-right .attributes-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

/* 4. 属性列表优化：像羊皮纸一样的条目 */
.equipment-attributes-right .attribute-item {
  display: flex;
  flex-direction: row;
  align-items: center;
  background: rgba(255, 255, 255, 0.03);
  border-radius: 8rpx;
  padding: 12rpx 16rpx;
  margin-bottom: 12rpx;
  border: 1rpx solid rgba(255, 255, 255, 0.05);
}

.equipment-attributes-right .attribute-item:last-child {
  margin-bottom: 0;
}

.equipment-attributes-right .label {
  color: #cccccc;
  font-size: 28rpx;
  margin-right: 12rpx;
  white-space: nowrap;
}

/* 5. 属性值优化 */
.equipment-attributes-right .value {
  color: #c9b065; /* 使用稍微柔和的暗金色 */
  font-family: monospace; /* 等宽字体更有数据感 */
  font-size: 28rpx;
  font-weight: bold;
  line-height: 1.4;
  text-align: left;
  flex: 1;
}

/* 头部区域的流光效果 */
.equipment-header::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(212, 175, 55, 0.1), transparent);
  animation: shimmer 3s infinite;
}

@keyframes shimmer {
  0% { left: -100%; }
  100% { left: 100%; }
}

.equipment-header:active {
  transform: scale(0.98);
  border-color: #ffd700;
  box-shadow: 
    0 0 30rpx rgba(255, 215, 0, 0.4),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.2);
}

.equipment-icon {
  width: 120rpx;
  height: 120rpx;
  margin-bottom: 20rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.icon-image {
  width: 100%;
  height: 100%;
  object-fit: contain;
}

.info {
  display: flex;
  flex-direction: column;
  align-items: center;
}

/* 3. 装备名称的大标题优化 */
.equipment-info-bottom-left .name {
  font-size: 40rpx;
  font-weight: 800; /* 更粗的字体 */
  background: linear-gradient(180deg, #d4af37 0%, #8a7328 100%); /* 金色渐变文字 */
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  text-shadow: 0 4rpx 10rpx rgba(0, 0, 0, 0.5);
  margin-bottom: 16rpx;
}

.type {
  display: block;
  font-size: 30rpx;
  color: #cccccc;
  margin-bottom: 18rpx;
}

.status {
  display: inline-block;
  padding: 10rpx 20rpx;
  border-radius: 20rpx;
  font-size: 26rpx;
  font-weight: bold;
}

/* 6. 激活状态标签美化 */
.status.activated {
  background: linear-gradient(90deg, #d4af37, #f2d45c);
  color: #000;
  font-weight: bold;
  border: none;
  box-shadow: 0 0 15rpx rgba(212, 175, 55, 0.6);
  padding: 6rpx 24rpx;
}

.status.not-activated {
  background: rgba(244, 67, 54, 0.2);
  color: #f44336;
  border: 1rpx solid rgba(244, 67, 54, 0.5);
}

/* 图片区域 */
.image-section {
  margin-bottom: 30rpx;
}

.image-preview {
  text-align: center;
}

.equipment-image {
  width: 300rpx;
  height: 300rpx;
  border-radius: 16rpx;
  transition: all 0.3s ease;
  border: 2rpx solid #d4af37;
}

.image-controls {
  margin-top: 20rpx;
  display: flex;
  gap: 20rpx;
  justify-content: center;
}

.btn-view, .btn-share, .btn-upload {
  background: linear-gradient(135deg, #d4af37 0%, #b8941f 100%);
  color: #000000;
  border: none;
  border-radius: 8rpx;
  padding: 16rpx 24rpx;
  font-size: 26rpx;
  font-weight: bold;
}

.btn-view:active, .btn-share:active, .btn-upload:active {
  opacity: 0.8;
}

/* 禁用按钮样式 */
.btn-disabled {
  background: linear-gradient(135deg, #666666 0%, #555555 100%) !important;
  color: #999999 !important;
  cursor: not-allowed !important;
  opacity: 0.6 !important;
}

.btn-disabled:active {
  opacity: 0.6 !important;
  transform: none !important;
}

/* 禁用按钮样式 */
.btn-disabled {
  background: linear-gradient(135deg, #666666 0%, #555555 100%) !important;
  color: #999999 !important;
  cursor: not-allowed !important;
  opacity: 0.6 !important;
}

.btn-disabled:active {
  opacity: 0.6 !important;
  transform: none !important;
}

/* 禁用按钮样式 */
.btn-disabled {
  background: linear-gradient(135deg, #666666 0%, #555555 100%) !important;
  color: #999999 !important;
  cursor: not-allowed !important;
  opacity: 0.6 !important;
}

.btn-disabled:active {
  opacity: 0.6 !important;
  transform: none !important;
}

/* 禁用按钮样式 */
.btn-disabled {
  background: linear-gradient(135deg, #666666 0%, #555555 100%) !important;
  color: #999999 !important;
  cursor: not-allowed !important;
  opacity: 0.6 !important;
}

.btn-disabled:active {
  opacity: 0.6 !important;
  transform: none !important;
}

.no-image {
  text-align: center;
  padding: 60rpx 30rpx;
  background: rgba(255, 255, 255, 0.05);
  border-radius: 16rpx;
  border: 1rpx dashed #666666;
}

.no-image-icon {
  font-size: 80rpx;
  margin-bottom: 20rpx;
}

.no-image-text {
  display: block;
  color: #999999;
  font-size: 28rpx;
  margin-bottom: 30rpx;
}

/* 属性区域 */
.attributes-section, .activation-info, .description-section {
  background: rgba(10, 10, 10, 0.95);
  border: 2rpx solid #d4af37;
  border-radius: 16rpx;
  padding: 30rpx;
  margin-bottom: 30rpx;
  transition: all 0.3s ease;
  box-shadow: 
    0 4rpx 20rpx rgba(0, 0, 0, 0.5),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.1);
}

.attributes-section:active, .activation-info:active, .description-section:active {
  transform: scale(0.98);
  border-color: #ffd700;
  box-shadow: 
    0 0 30rpx rgba(255, 215, 0, 0.4),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.2);
}

.section-title {
  font-size: 32rpx;
  font-weight: bold;
  color: #d4af37;
  margin-bottom: 20rpx;
  border-bottom: 1rpx solid rgba(212, 175, 55, 0.3);
  padding-bottom: 10rpx;
}

.attributes-list, .activation-details {
  display: flex;
  flex-direction: column;
  gap: 15rpx;
}

.attribute-item, .activation-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 15rpx 0;
  border-bottom: 1rpx solid rgba(255, 255, 255, 0.1);
}

.attribute-item:last-child, .activation-item:last-child {
  border-bottom: none;
}

.label {
  color: #cccccc;
  font-size: 28rpx;
}

.value {
  color: #ffffff;
  font-size: 28rpx;
  font-weight: bold;
}

/* 收藏者区域 */
.collector-section {
  background: rgba(10, 10, 10, 0.95);
  border: 2rpx solid #d4af37;
  border-radius: 16rpx;
  padding: 30rpx;
  margin-bottom: 30rpx;
  transition: all 0.3s ease;
  box-shadow: 
    0 4rpx 20rpx rgba(0, 0, 0, 0.5),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.1);
}

.collector-section:active {
  transform: scale(0.98);
  border-color: #ffd700;
  box-shadow: 
    0 0 30rpx rgba(255, 215, 0, 0.4),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.2);
}

.collector-content {
  display: flex;
  justify-content: center;
}

.collector-info {
  display: flex;
  align-items: center;
  gap: 20rpx;
}

.collector-avatar {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  border: 2rpx solid #d4af37;
}

.collector-nickname {
  font-size: 32rpx;
  color: #ffffff;
  font-weight: bold;
}

/* 收藏者区域 */
.collector-section {
  background: rgba(10, 10, 10, 0.95);
  border: 2rpx solid #d4af37;
  border-radius: 16rpx;
  padding: 30rpx;
  margin-bottom: 30rpx;
  transition: all 0.3s ease;
  box-shadow: 
    0 4rpx 20rpx rgba(0, 0, 0, 0.5),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.1);
}

.collector-section:active {
  transform: scale(0.98);
  border-color: #ffd700;
  box-shadow: 
    0 0 30rpx rgba(255, 215, 0, 0.4),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.2);
}

.collector-content {
  display: flex;
  justify-content: center;
}

.collector-info {
  display: flex;
  align-items: center;
  gap: 20rpx;
}

.collector-avatar {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  border: 2rpx solid #d4af37;
}

.collector-nickname {
  font-size: 32rpx;
  color: #ffffff;
  font-weight: bold;
}

/* 描述区域 */
.description-content {
  color: #cccccc;
  font-size: 28rpx;
  line-height: 1.6;
}

/* 未找到装备 */
.not-found {
  text-align: center;
  padding: 100rpx 30rpx;
}

.not-found-icon {
  font-size: 100rpx;
  margin-bottom: 30rpx;
}

.not-found-text {
  display: block;
  color: #999999;
  font-size: 32rpx;
  margin-bottom: 40rpx;
}

.btn-back {
  background: #666666;
  color: #ffffff;
  border: none;
  border-radius: 8rpx;
  padding: 20rpx 40rpx;
  font-size: 28rpx;
}

/* 所有卡片都使用横向布局 */
.attributes-section, .images-section, .collector-section {
  background: rgba(10, 10, 10, 0.95);
  border: 2rpx solid #d4af37;
  border-radius: 16rpx;
  padding: 30rpx;
  margin-bottom: 30rpx;
  transition: all 0.3s ease;
  box-shadow: 
    0 4rpx 20rpx rgba(0, 0, 0, 0.5),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.1);
}

.attributes-section:active, .images-section:active, .collector-section:active {
  transform: scale(0.98);
  border-color: #ffd700;
  box-shadow: 
    0 0 30rpx rgba(255, 215, 0, 0.4),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.2);
}

/* 多图片预览卡片 - 横向布局 */
.images-section {
  min-height: 220rpx;
}

.images-container {
  width: 100%;
  height: 140rpx;
  position: relative;
  overflow: hidden;
}

.images-scroll {
  width: 100%;
  height: 140rpx;
  white-space: nowrap;
  scrollbar-width: none;
  -ms-overflow-style: none;
}

.images-scroll::-webkit-scrollbar {
  display: none;
}

.images-list {
  display: inline-flex;
  gap: 20rpx;
  padding: 10rpx 0;
  height: 120rpx;
}

.image-thumbnail {
  position: relative;
  width: 120rpx;
  height: 120rpx;
  border-radius: 12rpx;
  overflow: hidden;
  border: 3rpx solid #d4af37;
  transition: all 0.3s ease;
  cursor: pointer;
  background: rgba(0, 0, 0, 0.3);
}

.image-thumbnail:active {
  transform: scale(0.95);
  border-color: #ffd700;
  box-shadow: 0 0 20rpx rgba(255, 215, 0, 0.6);
}

.thumbnail-image {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.thumbnail-overlay {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: rgba(212, 175, 55, 0.9);
  padding: 2rpx;
  text-align: center;
  height: 20rpx;
}

.thumbnail-text {
  font-size: 22rpx;
  color: #000;
  font-weight: bold;
}

/* 收藏者卡片样式 */
.collector-content {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 20rpx;
  padding: 20rpx 0;
}

.collector-avatar {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  border: 3rpx solid #d4af37;
  box-shadow: 0 4rpx 15rpx rgba(212, 175, 55, 0.3);
}

.collector-info {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
}

.collector-nickname {
  font-size: 30rpx;
  color: #ffffff;
  font-weight: bold;
}

.collector-badge {
  font-size: 24rpx;
  color: #d4af37;
  background: rgba(212, 175, 55, 0.1);
  padding: 6rpx 12rpx;
  border-radius: 15rpx;
  border: 1rpx solid rgba(212, 175, 55, 0.3);
}

/* 用户属性区域样式 */
.user-attributes-section {
  background: rgba(10, 10, 10, 0.95);
  border: 2rpx solid #d4af37;
  border-radius: 16rpx;
  padding: 30rpx;
  margin-bottom: 30rpx;
  transition: all 0.3s ease;
  box-shadow: 
    0 4rpx 20rpx rgba(0, 0, 0, 0.5),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.1);
}

.user-attributes-section:active {
  transform: scale(0.98);
  border-color: #ffd700;
  box-shadow: 
    0 0 30rpx rgba(255, 215, 0, 0.4),
    inset 0 1rpx 0 rgba(255, 255, 255, 0.2);
}

.user-attributes-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.user-attribute-item {
  display: flex;
  align-items: center;
  background: rgba(255, 255, 255, 0.03);
  border-radius: 12rpx;
  padding: 20rpx;
  border: 1rpx solid rgba(212, 175, 55, 0.2);
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
}

.user-attribute-item:before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(212, 175, 55, 0.1), transparent);
  animation: shimmer 3s infinite;
}

.user-attribute-item:active {
  transform: scale(0.95);
  border-color: #ffd700;
  box-shadow: 0 0 20rpx rgba(255, 215, 0, 0.3);
}

.attribute-text {
  font-size: 28rpx;
  font-weight: bold;
  font-family: monospace;
  line-height: 1.4;
  flex: 1;
  text-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.5);
}